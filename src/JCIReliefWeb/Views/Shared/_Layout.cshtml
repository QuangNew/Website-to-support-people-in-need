<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - JCI Cứu Trợ</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome 6 -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts - Vietnamese -->
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Leaflet CSS (Free map library) -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    <div class="app-container">
        <!-- Sidebar Navigation -->
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <img src="~/images/jci-logo.png" alt="JCI Logo" class="logo-img" onerror="this.style.display='none'">
                    <span class="logo-text">JCI</span>
                </div>
            </div>
            
            <nav class="sidebar-nav">
                <ul class="nav-list">
                    <li class="nav-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")">
                        <a asp-controller="Home" asp-action="Index" class="nav-link">
                            <i class="fa-solid fa-house"></i>
                            <span>Danh sách</span>
                        </a>
                    </li>
                    <li class="nav-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Sponsor" ? "active" : "")">
                        <a asp-controller="Sponsor" asp-action="Index" class="nav-link">
                            <i class="fa-solid fa-heart"></i>
                            <span>Muốn cho</span>
                        </a>
                    </li>
                    <li class="nav-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "SOS" ? "active" : "")">
                        <a asp-controller="SOS" asp-action="Index" class="nav-link">
                            <i class="fa-solid fa-hand-holding-heart"></i>
                            <span>Cần giúp</span>
                        </a>
                    </li>
                    <li class="nav-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Map" ? "active" : "")">
                        <a asp-controller="Map" asp-action="Index" class="nav-link">
                            <i class="fa-solid fa-location-dot"></i>
                            <span>Chìm vị trí</span>
                        </a>
                    </li>
                    <li class="nav-item @(ViewContext.RouteData.Values["controller"]?.ToString() == "Volunteer" ? "active" : "")">
                        <a asp-controller="Volunteer" asp-action="Index" class="nav-link">
                            <i class="fa-solid fa-user-plus"></i>
                            <span>Đăng ký vai trò</span>
                        </a>
                    </li>
                    
                    <li class="nav-divider"></li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link" id="langToggle">
                            <i class="fa-solid fa-language"></i>
                            <span>Tiếng Việt</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link" data-bs-toggle="modal" data-bs-target="#settingsModal">
                            <i class="fa-solid fa-gear"></i>
                            <span>Cài đặt</span>
                        </a>
                    </li>
                </ul>
            </nav>
            
            <!-- Sidebar Footer -->
            <div class="sidebar-footer">
                <div class="campaign-badge">
                    <span class="badge-dot"></span>
                    <span>Chung lòng mùa lũ</span>
                </div>
            </div>
        </aside>
        
        <!-- Main Content Area -->
        <div class="main-wrapper">
            <!-- Top Header Bar -->
            <header class="top-header">
                <div class="header-left">
                    <button class="btn btn-link sidebar-toggle d-lg-none" id="sidebarToggle">
                        <i class="fa-solid fa-bars"></i>
                    </button>
                    <h1 class="page-title">@ViewData["Title"]</h1>
                </div>
                
                <div class="header-center">
                    <div class="search-box">
                        <i class="fa-solid fa-magnifying-glass"></i>
                        <input type="text" placeholder="Tìm kiếm địa điểm, chiến dịch..." class="search-input">
                    </div>
                </div>
                
                <div class="header-right">
                    <button class="header-btn" title="Trợ giúp">
                        <i class="fa-solid fa-circle-question"></i>
                    </button>
                    <button class="header-btn notification-btn" title="Thông báo">
                        <i class="fa-solid fa-bell"></i>
                        <span class="notification-badge">3</span>
                    </button>
                    <button class="header-btn" title="Tin nhắn">
                        <i class="fa-regular fa-comment-dots"></i>
                    </button>
                    <div class="dropdown">
                        <button class="header-btn user-btn" data-bs-toggle="dropdown">
                            <i class="fa-solid fa-users"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" asp-controller="Admin" asp-action="Index"><i class="fa-solid fa-shield-halved me-2"></i>Admin Panel</a></li>
                            <li><a class="dropdown-item" href="#"><i class="fa-solid fa-user me-2"></i>Hồ sơ</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#"><i class="fa-solid fa-right-from-bracket me-2"></i>Đăng xuất</a></li>
                        </ul>
                    </div>
                    <button class="header-btn user-avatar" title="Tài khoản">
                        <i class="fa-solid fa-user"></i>
                    </button>
                </div>
            </header>
            
            <!-- Page Content -->
            <main class="main-content">
                @RenderBody()
            </main>
        </div>
    </div>
    
    <!-- AI Chat Widget -->
    <div class="chat-widget" id="chatWidget">
        <button class="chat-toggle" id="chatToggle" title="Trợ lý AI">
            <i class="fa-solid fa-robot"></i>
        </button>
        <div class="chat-panel" id="chatPanel">
            <div class="chat-header">
                <div class="chat-title">
                    <i class="fa-solid fa-robot"></i>
                    <span>Trợ lý cứu trợ AI</span>
                </div>
                <button class="chat-close" id="chatClose">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>
            <div class="chat-messages" id="chatMessages">
                <div class="message bot">
                    <div class="message-content">
                        Xin chào! Tôi là trợ lý AI của JCI. Tôi có thể giúp bạn:
                        <ul>
                            <li>🆘 Hướng dẫn sơ cứu cơ bản</li>
                            <li>📍 Tìm điểm cứu trợ gần nhất</li>
                            <li>📞 Cung cấp số điện thoại khẩn cấp</li>
                            <li>💡 Mẹo sinh tồn trong thiên tai</li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="chat-input-area">
                <input type="text" placeholder="Nhập câu hỏi của bạn..." class="chat-input" id="chatInput">
                <button class="chat-send" id="chatSend">
                    <i class="fa-solid fa-paper-plane"></i>
                </button>
            </div>
            <div class="chat-emergency">
                <span>Khẩn cấp:</span>
                <a href="tel:113">113</a>
                <a href="tel:115">115</a>
                <a href="tel:114">114</a>
            </div>
        </div>
    </div>
    
    <!-- Settings Modal -->
    <div class="modal fade" id="settingsModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fa-solid fa-gear me-2"></i>Cài đặt</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="setting-item">
                        <label>Chế độ tối</label>
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="darkModeToggle">
                        </div>
                    </div>
                    <div class="setting-item">
                        <label>Thông báo</label>
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="notificationToggle" checked>
                        </div>
                    </div>
                    <div class="setting-item">
                        <label>Âm thanh cảnh báo</label>
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="soundToggle" checked>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
